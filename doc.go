//gpsd contains Go types (structs) wich defines how the encoding/json parser
//should interpret JSON documents from a gpsd (gps service) daemon
//(gpsd upstream website: http://www.catb.org/gpsd/ )
package gpsdjson
